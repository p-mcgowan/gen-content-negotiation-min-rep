summary: "Produces both html and json"
description: "Trying to get getSingleSuccessResponse template helper to put both produces in the routes file, didnt work"
x-passRequest: true
responses:
  "404":
    description: "Both not found"
  "202":
    description: "Success"
    content:
      text/html:
        schema:
          type: "string"
  "200":
    description: "Success"
    content:
      application/json:
        schema:
          type: "object"
          required:
            - "test"
          properties:
            test:
              type: "string"
