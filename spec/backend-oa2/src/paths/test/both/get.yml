x-passRequest: true
produces:
  - "application/json"
  - "text/html"
responses:
  "200":
    description: "Success"
    # can't really do this properly in oas2 with generate-it response mapping
    schema:
      type: "string"
